---
- name: Creates directory
  file:
    path: ~/web
    state: directory

- name: Move files
  become: true
  copy:
    src: "files/"
    dest: "~/web/"

- name: Executing node
  shell: |
    pm2 start ~/web/main.js -f