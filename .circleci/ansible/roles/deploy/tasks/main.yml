---
- name: Creates directory
  file:
    path: ~/web
    state: directory

- name: Move files
  template:
    src: "files/main.js"
    dest: "~/web/main.js"

- name: Executing node
  shell: |
    pm2 start ~/web/main.js -f