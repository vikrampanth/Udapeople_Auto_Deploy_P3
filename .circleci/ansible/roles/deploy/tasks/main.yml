---
- name: Creates directory
  file:
    path: ~/web
    state: directory

- name: Copy backend tar files
  become: true
  copy:
    src: /home/backend-dist.tar.gz
    dest: ~/web/backend-dist.tar.gz

- name: List file
  shell: |
    cd ~/web
    ls -la

- name: Unpack
  shell: |
    cd ~/web
    tar xzvf backend-dist.tar.gz
    mv root/project/backend/dist/ .

- name: Executing node
  shell: |
    pm2 start ~/web/main.js -f